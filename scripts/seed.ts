/**
 * Responsabilidade: popular dados de desenvolvimento/teste — usuário admin, roles base,
 * usuário de carga (load@iam.local) para os cenários k6.
 * Regras: idempotente; senhas de seed apenas em NODE_ENV != production (aborta em prod).
 */
