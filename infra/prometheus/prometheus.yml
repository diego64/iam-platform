global:
  scrape_interval: 15s
rule_files:
  - /etc/prometheus/alerts/*.yml
scrape_configs:
  - job_name: iam-platform
    static_configs:
      - targets: ["host.docker.internal:3000"]
    metrics_path: /metrics
